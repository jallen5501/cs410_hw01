John Allen
Exercise in running ant with junit cases on the command line.

default in buildJohn Allen
Exercise in running ant with junit cases on the command line.

default in build.xml is run-test. This target will set up
the file structure, comile all java files, and run junit
test cases
